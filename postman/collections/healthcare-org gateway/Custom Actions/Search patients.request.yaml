$kind: http-request
description: ilike over first_name, last_name, mrn.
url: "{{baseUrl}}/api/patients/search?q=reyes"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  q: reyes
order: 1000
