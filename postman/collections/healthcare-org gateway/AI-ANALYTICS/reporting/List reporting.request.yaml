$kind: http-request
description: List first 10 Reporting rows.
url: "{{baseUrl}}/api/reporting?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
