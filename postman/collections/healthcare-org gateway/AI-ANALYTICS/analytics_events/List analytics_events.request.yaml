$kind: http-request
description: List first 10 Analytics Events rows.
url: "{{baseUrl}}/api/analytics_events?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
