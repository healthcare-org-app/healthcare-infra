$kind: http-request
description: Get a single Care Teams by numeric id.
url: "{{baseUrl}}/api/care_teams/1"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
order: 3000
