$kind: http-request
description: List first 10 Clinical Decision Support rows.
url: "{{baseUrl}}/api/clinical_decision_support?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
