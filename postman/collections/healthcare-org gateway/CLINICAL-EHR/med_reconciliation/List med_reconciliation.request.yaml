$kind: http-request
description: List first 10 Med Reconciliation rows.
url: "{{baseUrl}}/api/med_reconciliation?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
