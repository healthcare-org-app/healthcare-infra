$kind: http-request
description: List first 10 Provider Directory rows.
url: "{{baseUrl}}/api/provider_directory?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
