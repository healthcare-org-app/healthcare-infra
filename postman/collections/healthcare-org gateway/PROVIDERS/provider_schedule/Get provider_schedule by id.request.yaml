$kind: http-request
description: Get a single Provider Schedule by numeric id.
url: "{{baseUrl}}/api/provider_schedule/1"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
order: 3000
