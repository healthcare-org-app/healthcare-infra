$kind: http-request
description: List first 10 Appointment Slots rows.
url: "{{baseUrl}}/api/appointment_slots?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
