$kind: http-request
description: List first 10 Waitlist rows.
url: "{{baseUrl}}/api/waitlist?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
