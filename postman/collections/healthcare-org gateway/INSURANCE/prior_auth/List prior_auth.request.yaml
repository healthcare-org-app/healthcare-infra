$kind: http-request
description: List first 10 Prior Auth rows.
url: "{{baseUrl}}/api/prior_auth?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
