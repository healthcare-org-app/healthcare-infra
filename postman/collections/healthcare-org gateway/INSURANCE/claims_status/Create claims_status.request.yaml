$kind: http-request
description: |-
  Create a Claims Status. Body fields mirror `services.ts` createFields.

  **FK references (fetch these first):** `claim_id` → `GET /api/claims_submission`
url: "{{baseUrl}}/api/claims_status"
method: POST
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "status": "active",
      "claim_id": "{{claim_id}}"
    }
order: 2000
