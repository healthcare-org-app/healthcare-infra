$kind: collection
description: |-
  Imaging Orders — CRUD at `/api/imaging_orders`.

  **FK references (fetch these first):** `patient_id` → `GET /api/patients`, `ordered_by` → `GET /api/providers`, `encounter_id` → `GET /api/encounters`
order: 3000
