$kind: http-request
description: Create a Secrets Vault. Body fields mirror `services.ts` createFields.
url: "{{baseUrl}}/api/secrets_vault"
method: POST
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "status": "active",
      "name": "sample name",
      "value": "Sample value"
    }
order: 2000
