$kind: http-request
description: List first 10 Device Registry rows.
url: "{{baseUrl}}/api/device_registry?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
