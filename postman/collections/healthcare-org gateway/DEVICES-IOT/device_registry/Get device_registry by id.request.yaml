$kind: http-request
description: Get a single Device Registry by numeric id.
url: "{{baseUrl}}/api/device_registry/1"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
order: 3000
