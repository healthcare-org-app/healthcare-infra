$kind: http-request
description: List first 10 Device Fleet rows.
url: "{{baseUrl}}/api/device_fleet?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
