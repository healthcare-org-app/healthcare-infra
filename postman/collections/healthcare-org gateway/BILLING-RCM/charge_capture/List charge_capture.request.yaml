$kind: http-request
description: List first 10 Charge Capture rows.
url: "{{baseUrl}}/api/charge_capture?limit=10"
method: GET
headers:
  Authorization: Bearer {{apiKey}}
  Content-Type: application/json
queryParams:
  limit: "10"
order: 1000
