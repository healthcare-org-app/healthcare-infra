-- Auto-generated by tools/generate_supabase_schema.py. Paste into Supabase SQL Editor.
-- Idempotent: safe to re-run.

-- Shared updated_at trigger function.
create or replace function public.set_updated_at()
returns trigger
language plpgsql
as $$
begin
  new.updated_at = now();
  return new;
end;
$$;

-- identity-service → /rest/v1/identity
create table if not exists public.identity (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists identity_data_gin on public.identity using gin (data);
create index if not exists identity_status_idx on public.identity (status);

drop trigger if exists identity_set_updated_at on public.identity;
create trigger identity_set_updated_at before update on public.identity
  for each row execute function public.set_updated_at();

alter table public.identity enable row level security;
drop policy if exists anon_all on public.identity;
drop policy if exists authenticated_all on public.identity;
create policy authenticated_all on public.identity
  for all
  to authenticated
  using (true)
  with check (true);
-- auth-service → /rest/v1/auth
create table if not exists public.auth (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists auth_data_gin on public.auth using gin (data);
create index if not exists auth_status_idx on public.auth (status);

drop trigger if exists auth_set_updated_at on public.auth;
create trigger auth_set_updated_at before update on public.auth
  for each row execute function public.set_updated_at();

alter table public.auth enable row level security;
drop policy if exists anon_all on public.auth;
drop policy if exists authenticated_all on public.auth;
create policy authenticated_all on public.auth
  for all
  to authenticated
  using (true)
  with check (true);
-- authorization-service → /rest/v1/authorization_records
create table if not exists public.authorization_records (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists authorization_records_data_gin on public.authorization_records using gin (data);
create index if not exists authorization_records_status_idx on public.authorization_records (status);

drop trigger if exists authorization_records_set_updated_at on public.authorization_records;
create trigger authorization_records_set_updated_at before update on public.authorization_records
  for each row execute function public.set_updated_at();

alter table public.authorization_records enable row level security;
drop policy if exists anon_all on public.authorization_records;
drop policy if exists authenticated_all on public.authorization_records;
create policy authenticated_all on public.authorization_records
  for all
  to authenticated
  using (true)
  with check (true);
-- config-service → /rest/v1/config
create table if not exists public.config (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists config_data_gin on public.config using gin (data);
create index if not exists config_status_idx on public.config (status);

drop trigger if exists config_set_updated_at on public.config;
create trigger config_set_updated_at before update on public.config
  for each row execute function public.set_updated_at();

alter table public.config enable row level security;
drop policy if exists anon_all on public.config;
drop policy if exists authenticated_all on public.config;
create policy authenticated_all on public.config
  for all
  to authenticated
  using (true)
  with check (true);
-- secrets-vault → /rest/v1/secrets_vault
create table if not exists public.secrets_vault (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists secrets_vault_data_gin on public.secrets_vault using gin (data);
create index if not exists secrets_vault_status_idx on public.secrets_vault (status);

drop trigger if exists secrets_vault_set_updated_at on public.secrets_vault;
create trigger secrets_vault_set_updated_at before update on public.secrets_vault
  for each row execute function public.set_updated_at();

alter table public.secrets_vault enable row level security;
drop policy if exists anon_all on public.secrets_vault;
drop policy if exists authenticated_all on public.secrets_vault;
create policy authenticated_all on public.secrets_vault
  for all
  to authenticated
  using (true)
  with check (true);
-- audit-log-service → /rest/v1/audit_log
create table if not exists public.audit_log (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists audit_log_data_gin on public.audit_log using gin (data);
create index if not exists audit_log_status_idx on public.audit_log (status);

drop trigger if exists audit_log_set_updated_at on public.audit_log;
create trigger audit_log_set_updated_at before update on public.audit_log
  for each row execute function public.set_updated_at();

alter table public.audit_log enable row level security;
drop policy if exists anon_all on public.audit_log;
drop policy if exists authenticated_all on public.audit_log;
create policy authenticated_all on public.audit_log
  for all
  to authenticated
  using (true)
  with check (true);
-- feature-flags-service → /rest/v1/feature_flags
create table if not exists public.feature_flags (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists feature_flags_data_gin on public.feature_flags using gin (data);
create index if not exists feature_flags_status_idx on public.feature_flags (status);

drop trigger if exists feature_flags_set_updated_at on public.feature_flags;
create trigger feature_flags_set_updated_at before update on public.feature_flags
  for each row execute function public.set_updated_at();

alter table public.feature_flags enable row level security;
drop policy if exists anon_all on public.feature_flags;
drop policy if exists authenticated_all on public.feature_flags;
create policy authenticated_all on public.feature_flags
  for all
  to authenticated
  using (true)
  with check (true);
-- tenancy-service → /rest/v1/tenancy
create table if not exists public.tenancy (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists tenancy_data_gin on public.tenancy using gin (data);
create index if not exists tenancy_status_idx on public.tenancy (status);

drop trigger if exists tenancy_set_updated_at on public.tenancy;
create trigger tenancy_set_updated_at before update on public.tenancy
  for each row execute function public.set_updated_at();

alter table public.tenancy enable row level security;
drop policy if exists anon_all on public.tenancy;
drop policy if exists authenticated_all on public.tenancy;
create policy authenticated_all on public.tenancy
  for all
  to authenticated
  using (true)
  with check (true);
-- patients-service → /rest/v1/patients
create table if not exists public.patients (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now(),
  first_name   text,
  last_name    text,
  dob          date,
  mrn          text unique,
  identity_sub text
);
create index if not exists patients_data_gin on public.patients using gin (data);
create index if not exists patients_status_idx on public.patients (status);
create index if not exists patients_name_idx on public.patients (lower(last_name), lower(first_name));
create index if not exists patients_mrn_idx on public.patients (mrn) where mrn is not null;

drop trigger if exists patients_set_updated_at on public.patients;
create trigger patients_set_updated_at before update on public.patients
  for each row execute function public.set_updated_at();

alter table public.patients enable row level security;
drop policy if exists anon_all on public.patients;
drop policy if exists authenticated_all on public.patients;
create policy authenticated_all on public.patients
  for all
  to authenticated
  using (true)
  with check (true);
-- demographics-service → /rest/v1/demographics
create table if not exists public.demographics (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists demographics_data_gin on public.demographics using gin (data);
create index if not exists demographics_status_idx on public.demographics (status);

drop trigger if exists demographics_set_updated_at on public.demographics;
create trigger demographics_set_updated_at before update on public.demographics
  for each row execute function public.set_updated_at();

alter table public.demographics enable row level security;
drop policy if exists anon_all on public.demographics;
drop policy if exists authenticated_all on public.demographics;
create policy authenticated_all on public.demographics
  for all
  to authenticated
  using (true)
  with check (true);
-- patient-consent-service → /rest/v1/patient_consent
create table if not exists public.patient_consent (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists patient_consent_data_gin on public.patient_consent using gin (data);
create index if not exists patient_consent_status_idx on public.patient_consent (status);

drop trigger if exists patient_consent_set_updated_at on public.patient_consent;
create trigger patient_consent_set_updated_at before update on public.patient_consent
  for each row execute function public.set_updated_at();

alter table public.patient_consent enable row level security;
drop policy if exists anon_all on public.patient_consent;
drop policy if exists authenticated_all on public.patient_consent;
create policy authenticated_all on public.patient_consent
  for all
  to authenticated
  using (true)
  with check (true);
-- patient-preferences-service → /rest/v1/patient_preferences
create table if not exists public.patient_preferences (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists patient_preferences_data_gin on public.patient_preferences using gin (data);
create index if not exists patient_preferences_status_idx on public.patient_preferences (status);

drop trigger if exists patient_preferences_set_updated_at on public.patient_preferences;
create trigger patient_preferences_set_updated_at before update on public.patient_preferences
  for each row execute function public.set_updated_at();

alter table public.patient_preferences enable row level security;
drop policy if exists anon_all on public.patient_preferences;
drop policy if exists authenticated_all on public.patient_preferences;
create policy authenticated_all on public.patient_preferences
  for all
  to authenticated
  using (true)
  with check (true);
-- patient-relationships-service → /rest/v1/patient_relationships
create table if not exists public.patient_relationships (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists patient_relationships_data_gin on public.patient_relationships using gin (data);
create index if not exists patient_relationships_status_idx on public.patient_relationships (status);

drop trigger if exists patient_relationships_set_updated_at on public.patient_relationships;
create trigger patient_relationships_set_updated_at before update on public.patient_relationships
  for each row execute function public.set_updated_at();

alter table public.patient_relationships enable row level security;
drop policy if exists anon_all on public.patient_relationships;
drop policy if exists authenticated_all on public.patient_relationships;
create policy authenticated_all on public.patient_relationships
  for all
  to authenticated
  using (true)
  with check (true);
-- patient-timeline-service → /rest/v1/patient_timeline
create table if not exists public.patient_timeline (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists patient_timeline_data_gin on public.patient_timeline using gin (data);
create index if not exists patient_timeline_status_idx on public.patient_timeline (status);

drop trigger if exists patient_timeline_set_updated_at on public.patient_timeline;
create trigger patient_timeline_set_updated_at before update on public.patient_timeline
  for each row execute function public.set_updated_at();

alter table public.patient_timeline enable row level security;
drop policy if exists anon_all on public.patient_timeline;
drop policy if exists authenticated_all on public.patient_timeline;
create policy authenticated_all on public.patient_timeline
  for all
  to authenticated
  using (true)
  with check (true);
-- patient-search-service → /rest/v1/patient_search
create table if not exists public.patient_search (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists patient_search_data_gin on public.patient_search using gin (data);
create index if not exists patient_search_status_idx on public.patient_search (status);

drop trigger if exists patient_search_set_updated_at on public.patient_search;
create trigger patient_search_set_updated_at before update on public.patient_search
  for each row execute function public.set_updated_at();

alter table public.patient_search enable row level security;
drop policy if exists anon_all on public.patient_search;
drop policy if exists authenticated_all on public.patient_search;
create policy authenticated_all on public.patient_search
  for all
  to authenticated
  using (true)
  with check (true);
-- patient-merge-service → /rest/v1/patient_merge
create table if not exists public.patient_merge (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists patient_merge_data_gin on public.patient_merge using gin (data);
create index if not exists patient_merge_status_idx on public.patient_merge (status);

drop trigger if exists patient_merge_set_updated_at on public.patient_merge;
create trigger patient_merge_set_updated_at before update on public.patient_merge
  for each row execute function public.set_updated_at();

alter table public.patient_merge enable row level security;
drop policy if exists anon_all on public.patient_merge;
drop policy if exists authenticated_all on public.patient_merge;
create policy authenticated_all on public.patient_merge
  for all
  to authenticated
  using (true)
  with check (true);
-- patient-communications-service → /rest/v1/patient_communications
create table if not exists public.patient_communications (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists patient_communications_data_gin on public.patient_communications using gin (data);
create index if not exists patient_communications_status_idx on public.patient_communications (status);

drop trigger if exists patient_communications_set_updated_at on public.patient_communications;
create trigger patient_communications_set_updated_at before update on public.patient_communications
  for each row execute function public.set_updated_at();

alter table public.patient_communications enable row level security;
drop policy if exists anon_all on public.patient_communications;
drop policy if exists authenticated_all on public.patient_communications;
create policy authenticated_all on public.patient_communications
  for all
  to authenticated
  using (true)
  with check (true);
-- providers-service → /rest/v1/providers
create table if not exists public.providers (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists providers_data_gin on public.providers using gin (data);
create index if not exists providers_status_idx on public.providers (status);

drop trigger if exists providers_set_updated_at on public.providers;
create trigger providers_set_updated_at before update on public.providers
  for each row execute function public.set_updated_at();

alter table public.providers enable row level security;
drop policy if exists anon_all on public.providers;
drop policy if exists authenticated_all on public.providers;
create policy authenticated_all on public.providers
  for all
  to authenticated
  using (true)
  with check (true);
-- credentialing-service → /rest/v1/credentialing
create table if not exists public.credentialing (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists credentialing_data_gin on public.credentialing using gin (data);
create index if not exists credentialing_status_idx on public.credentialing (status);

drop trigger if exists credentialing_set_updated_at on public.credentialing;
create trigger credentialing_set_updated_at before update on public.credentialing
  for each row execute function public.set_updated_at();

alter table public.credentialing enable row level security;
drop policy if exists anon_all on public.credentialing;
drop policy if exists authenticated_all on public.credentialing;
create policy authenticated_all on public.credentialing
  for all
  to authenticated
  using (true)
  with check (true);
-- licensing-service → /rest/v1/licensing
create table if not exists public.licensing (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists licensing_data_gin on public.licensing using gin (data);
create index if not exists licensing_status_idx on public.licensing (status);

drop trigger if exists licensing_set_updated_at on public.licensing;
create trigger licensing_set_updated_at before update on public.licensing
  for each row execute function public.set_updated_at();

alter table public.licensing enable row level security;
drop policy if exists anon_all on public.licensing;
drop policy if exists authenticated_all on public.licensing;
create policy authenticated_all on public.licensing
  for all
  to authenticated
  using (true)
  with check (true);
-- specialties-service → /rest/v1/specialties
create table if not exists public.specialties (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists specialties_data_gin on public.specialties using gin (data);
create index if not exists specialties_status_idx on public.specialties (status);

drop trigger if exists specialties_set_updated_at on public.specialties;
create trigger specialties_set_updated_at before update on public.specialties
  for each row execute function public.set_updated_at();

alter table public.specialties enable row level security;
drop policy if exists anon_all on public.specialties;
drop policy if exists authenticated_all on public.specialties;
create policy authenticated_all on public.specialties
  for all
  to authenticated
  using (true)
  with check (true);
-- provider-schedule-service → /rest/v1/provider_schedule
create table if not exists public.provider_schedule (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists provider_schedule_data_gin on public.provider_schedule using gin (data);
create index if not exists provider_schedule_status_idx on public.provider_schedule (status);

drop trigger if exists provider_schedule_set_updated_at on public.provider_schedule;
create trigger provider_schedule_set_updated_at before update on public.provider_schedule
  for each row execute function public.set_updated_at();

alter table public.provider_schedule enable row level security;
drop policy if exists anon_all on public.provider_schedule;
drop policy if exists authenticated_all on public.provider_schedule;
create policy authenticated_all on public.provider_schedule
  for all
  to authenticated
  using (true)
  with check (true);
-- on-call-service → /rest/v1/on_call
create table if not exists public.on_call (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists on_call_data_gin on public.on_call using gin (data);
create index if not exists on_call_status_idx on public.on_call (status);

drop trigger if exists on_call_set_updated_at on public.on_call;
create trigger on_call_set_updated_at before update on public.on_call
  for each row execute function public.set_updated_at();

alter table public.on_call enable row level security;
drop policy if exists anon_all on public.on_call;
drop policy if exists authenticated_all on public.on_call;
create policy authenticated_all on public.on_call
  for all
  to authenticated
  using (true)
  with check (true);
-- provider-directory → /rest/v1/provider_directory
create table if not exists public.provider_directory (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists provider_directory_data_gin on public.provider_directory using gin (data);
create index if not exists provider_directory_status_idx on public.provider_directory (status);

drop trigger if exists provider_directory_set_updated_at on public.provider_directory;
create trigger provider_directory_set_updated_at before update on public.provider_directory
  for each row execute function public.set_updated_at();

alter table public.provider_directory enable row level security;
drop policy if exists anon_all on public.provider_directory;
drop policy if exists authenticated_all on public.provider_directory;
create policy authenticated_all on public.provider_directory
  for all
  to authenticated
  using (true)
  with check (true);
-- provider-performance-service → /rest/v1/provider_performance
create table if not exists public.provider_performance (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists provider_performance_data_gin on public.provider_performance using gin (data);
create index if not exists provider_performance_status_idx on public.provider_performance (status);

drop trigger if exists provider_performance_set_updated_at on public.provider_performance;
create trigger provider_performance_set_updated_at before update on public.provider_performance
  for each row execute function public.set_updated_at();

alter table public.provider_performance enable row level security;
drop policy if exists anon_all on public.provider_performance;
drop policy if exists authenticated_all on public.provider_performance;
create policy authenticated_all on public.provider_performance
  for all
  to authenticated
  using (true)
  with check (true);
-- ehr-service → /rest/v1/ehr
create table if not exists public.ehr (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists ehr_data_gin on public.ehr using gin (data);
create index if not exists ehr_status_idx on public.ehr (status);

drop trigger if exists ehr_set_updated_at on public.ehr;
create trigger ehr_set_updated_at before update on public.ehr
  for each row execute function public.set_updated_at();

alter table public.ehr enable row level security;
drop policy if exists anon_all on public.ehr;
drop policy if exists authenticated_all on public.ehr;
create policy authenticated_all on public.ehr
  for all
  to authenticated
  using (true)
  with check (true);
-- encounters-service → /rest/v1/encounters
create table if not exists public.encounters (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists encounters_data_gin on public.encounters using gin (data);
create index if not exists encounters_status_idx on public.encounters (status);

drop trigger if exists encounters_set_updated_at on public.encounters;
create trigger encounters_set_updated_at before update on public.encounters
  for each row execute function public.set_updated_at();

alter table public.encounters enable row level security;
drop policy if exists anon_all on public.encounters;
drop policy if exists authenticated_all on public.encounters;
create policy authenticated_all on public.encounters
  for all
  to authenticated
  using (true)
  with check (true);
-- clinical-notes-service → /rest/v1/clinical_notes
create table if not exists public.clinical_notes (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists clinical_notes_data_gin on public.clinical_notes using gin (data);
create index if not exists clinical_notes_status_idx on public.clinical_notes (status);

drop trigger if exists clinical_notes_set_updated_at on public.clinical_notes;
create trigger clinical_notes_set_updated_at before update on public.clinical_notes
  for each row execute function public.set_updated_at();

alter table public.clinical_notes enable row level security;
drop policy if exists anon_all on public.clinical_notes;
drop policy if exists authenticated_all on public.clinical_notes;
create policy authenticated_all on public.clinical_notes
  for all
  to authenticated
  using (true)
  with check (true);
-- problem-list-service → /rest/v1/problem_list
create table if not exists public.problem_list (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists problem_list_data_gin on public.problem_list using gin (data);
create index if not exists problem_list_status_idx on public.problem_list (status);

drop trigger if exists problem_list_set_updated_at on public.problem_list;
create trigger problem_list_set_updated_at before update on public.problem_list
  for each row execute function public.set_updated_at();

alter table public.problem_list enable row level security;
drop policy if exists anon_all on public.problem_list;
drop policy if exists authenticated_all on public.problem_list;
create policy authenticated_all on public.problem_list
  for all
  to authenticated
  using (true)
  with check (true);
-- med-reconciliation-service → /rest/v1/med_reconciliation
create table if not exists public.med_reconciliation (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists med_reconciliation_data_gin on public.med_reconciliation using gin (data);
create index if not exists med_reconciliation_status_idx on public.med_reconciliation (status);

drop trigger if exists med_reconciliation_set_updated_at on public.med_reconciliation;
create trigger med_reconciliation_set_updated_at before update on public.med_reconciliation
  for each row execute function public.set_updated_at();

alter table public.med_reconciliation enable row level security;
drop policy if exists anon_all on public.med_reconciliation;
drop policy if exists authenticated_all on public.med_reconciliation;
create policy authenticated_all on public.med_reconciliation
  for all
  to authenticated
  using (true)
  with check (true);
-- allergies-service → /rest/v1/allergies
create table if not exists public.allergies (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists allergies_data_gin on public.allergies using gin (data);
create index if not exists allergies_status_idx on public.allergies (status);

drop trigger if exists allergies_set_updated_at on public.allergies;
create trigger allergies_set_updated_at before update on public.allergies
  for each row execute function public.set_updated_at();

alter table public.allergies enable row level security;
drop policy if exists anon_all on public.allergies;
drop policy if exists authenticated_all on public.allergies;
create policy authenticated_all on public.allergies
  for all
  to authenticated
  using (true)
  with check (true);
-- immunizations-service → /rest/v1/immunizations
create table if not exists public.immunizations (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists immunizations_data_gin on public.immunizations using gin (data);
create index if not exists immunizations_status_idx on public.immunizations (status);

drop trigger if exists immunizations_set_updated_at on public.immunizations;
create trigger immunizations_set_updated_at before update on public.immunizations
  for each row execute function public.set_updated_at();

alter table public.immunizations enable row level security;
drop policy if exists anon_all on public.immunizations;
drop policy if exists authenticated_all on public.immunizations;
create policy authenticated_all on public.immunizations
  for all
  to authenticated
  using (true)
  with check (true);
-- vitals-service → /rest/v1/vitals
create table if not exists public.vitals (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists vitals_data_gin on public.vitals using gin (data);
create index if not exists vitals_status_idx on public.vitals (status);

drop trigger if exists vitals_set_updated_at on public.vitals;
create trigger vitals_set_updated_at before update on public.vitals
  for each row execute function public.set_updated_at();

alter table public.vitals enable row level security;
drop policy if exists anon_all on public.vitals;
drop policy if exists authenticated_all on public.vitals;
create policy authenticated_all on public.vitals
  for all
  to authenticated
  using (true)
  with check (true);
-- cpoe-service → /rest/v1/cpoe
create table if not exists public.cpoe (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists cpoe_data_gin on public.cpoe using gin (data);
create index if not exists cpoe_status_idx on public.cpoe (status);

drop trigger if exists cpoe_set_updated_at on public.cpoe;
create trigger cpoe_set_updated_at before update on public.cpoe
  for each row execute function public.set_updated_at();

alter table public.cpoe enable row level security;
drop policy if exists anon_all on public.cpoe;
drop policy if exists authenticated_all on public.cpoe;
create policy authenticated_all on public.cpoe
  for all
  to authenticated
  using (true)
  with check (true);
-- care-plan-service → /rest/v1/care_plan
create table if not exists public.care_plan (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists care_plan_data_gin on public.care_plan using gin (data);
create index if not exists care_plan_status_idx on public.care_plan (status);

drop trigger if exists care_plan_set_updated_at on public.care_plan;
create trigger care_plan_set_updated_at before update on public.care_plan
  for each row execute function public.set_updated_at();

alter table public.care_plan enable row level security;
drop policy if exists anon_all on public.care_plan;
drop policy if exists authenticated_all on public.care_plan;
create policy authenticated_all on public.care_plan
  for all
  to authenticated
  using (true)
  with check (true);
-- referrals-service → /rest/v1/referrals
create table if not exists public.referrals (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists referrals_data_gin on public.referrals using gin (data);
create index if not exists referrals_status_idx on public.referrals (status);

drop trigger if exists referrals_set_updated_at on public.referrals;
create trigger referrals_set_updated_at before update on public.referrals
  for each row execute function public.set_updated_at();

alter table public.referrals enable row level security;
drop policy if exists anon_all on public.referrals;
drop policy if exists authenticated_all on public.referrals;
create policy authenticated_all on public.referrals
  for all
  to authenticated
  using (true)
  with check (true);
-- discharge-summary-service → /rest/v1/discharge_summary
create table if not exists public.discharge_summary (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists discharge_summary_data_gin on public.discharge_summary using gin (data);
create index if not exists discharge_summary_status_idx on public.discharge_summary (status);

drop trigger if exists discharge_summary_set_updated_at on public.discharge_summary;
create trigger discharge_summary_set_updated_at before update on public.discharge_summary
  for each row execute function public.set_updated_at();

alter table public.discharge_summary enable row level security;
drop policy if exists anon_all on public.discharge_summary;
drop policy if exists authenticated_all on public.discharge_summary;
create policy authenticated_all on public.discharge_summary
  for all
  to authenticated
  using (true)
  with check (true);
-- clinical-decision-support → /rest/v1/clinical_decision_support
create table if not exists public.clinical_decision_support (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists clinical_decision_support_data_gin on public.clinical_decision_support using gin (data);
create index if not exists clinical_decision_support_status_idx on public.clinical_decision_support (status);

drop trigger if exists clinical_decision_support_set_updated_at on public.clinical_decision_support;
create trigger clinical_decision_support_set_updated_at before update on public.clinical_decision_support
  for each row execute function public.set_updated_at();

alter table public.clinical_decision_support enable row level security;
drop policy if exists anon_all on public.clinical_decision_support;
drop policy if exists authenticated_all on public.clinical_decision_support;
create policy authenticated_all on public.clinical_decision_support
  for all
  to authenticated
  using (true)
  with check (true);
-- diagnosis-codes-service → /rest/v1/diagnosis_codes
create table if not exists public.diagnosis_codes (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists diagnosis_codes_data_gin on public.diagnosis_codes using gin (data);
create index if not exists diagnosis_codes_status_idx on public.diagnosis_codes (status);

drop trigger if exists diagnosis_codes_set_updated_at on public.diagnosis_codes;
create trigger diagnosis_codes_set_updated_at before update on public.diagnosis_codes
  for each row execute function public.set_updated_at();

alter table public.diagnosis_codes enable row level security;
drop policy if exists anon_all on public.diagnosis_codes;
drop policy if exists authenticated_all on public.diagnosis_codes;
create policy authenticated_all on public.diagnosis_codes
  for all
  to authenticated
  using (true)
  with check (true);
-- care-teams-service → /rest/v1/care_teams
create table if not exists public.care_teams (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists care_teams_data_gin on public.care_teams using gin (data);
create index if not exists care_teams_status_idx on public.care_teams (status);

drop trigger if exists care_teams_set_updated_at on public.care_teams;
create trigger care_teams_set_updated_at before update on public.care_teams
  for each row execute function public.set_updated_at();

alter table public.care_teams enable row level security;
drop policy if exists anon_all on public.care_teams;
drop policy if exists authenticated_all on public.care_teams;
create policy authenticated_all on public.care_teams
  for all
  to authenticated
  using (true)
  with check (true);
-- lab-orders-service → /rest/v1/lab_orders
create table if not exists public.lab_orders (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists lab_orders_data_gin on public.lab_orders using gin (data);
create index if not exists lab_orders_status_idx on public.lab_orders (status);

drop trigger if exists lab_orders_set_updated_at on public.lab_orders;
create trigger lab_orders_set_updated_at before update on public.lab_orders
  for each row execute function public.set_updated_at();

alter table public.lab_orders enable row level security;
drop policy if exists anon_all on public.lab_orders;
drop policy if exists authenticated_all on public.lab_orders;
create policy authenticated_all on public.lab_orders
  for all
  to authenticated
  using (true)
  with check (true);
-- lab-results-service → /rest/v1/lab_results
create table if not exists public.lab_results (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists lab_results_data_gin on public.lab_results using gin (data);
create index if not exists lab_results_status_idx on public.lab_results (status);

drop trigger if exists lab_results_set_updated_at on public.lab_results;
create trigger lab_results_set_updated_at before update on public.lab_results
  for each row execute function public.set_updated_at();

alter table public.lab_results enable row level security;
drop policy if exists anon_all on public.lab_results;
drop policy if exists authenticated_all on public.lab_results;
create policy authenticated_all on public.lab_results
  for all
  to authenticated
  using (true)
  with check (true);
-- imaging-orders-service → /rest/v1/imaging_orders
create table if not exists public.imaging_orders (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists imaging_orders_data_gin on public.imaging_orders using gin (data);
create index if not exists imaging_orders_status_idx on public.imaging_orders (status);

drop trigger if exists imaging_orders_set_updated_at on public.imaging_orders;
create trigger imaging_orders_set_updated_at before update on public.imaging_orders
  for each row execute function public.set_updated_at();

alter table public.imaging_orders enable row level security;
drop policy if exists anon_all on public.imaging_orders;
drop policy if exists authenticated_all on public.imaging_orders;
create policy authenticated_all on public.imaging_orders
  for all
  to authenticated
  using (true)
  with check (true);
-- imaging-results-service → /rest/v1/imaging_results
create table if not exists public.imaging_results (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists imaging_results_data_gin on public.imaging_results using gin (data);
create index if not exists imaging_results_status_idx on public.imaging_results (status);

drop trigger if exists imaging_results_set_updated_at on public.imaging_results;
create trigger imaging_results_set_updated_at before update on public.imaging_results
  for each row execute function public.set_updated_at();

alter table public.imaging_results enable row level security;
drop policy if exists anon_all on public.imaging_results;
drop policy if exists authenticated_all on public.imaging_results;
create policy authenticated_all on public.imaging_results
  for all
  to authenticated
  using (true)
  with check (true);
-- pathology-service → /rest/v1/pathology
create table if not exists public.pathology (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists pathology_data_gin on public.pathology using gin (data);
create index if not exists pathology_status_idx on public.pathology (status);

drop trigger if exists pathology_set_updated_at on public.pathology;
create trigger pathology_set_updated_at before update on public.pathology
  for each row execute function public.set_updated_at();

alter table public.pathology enable row level security;
drop policy if exists anon_all on public.pathology;
drop policy if exists authenticated_all on public.pathology;
create policy authenticated_all on public.pathology
  for all
  to authenticated
  using (true)
  with check (true);
-- radiology-worklist → /rest/v1/radiology_worklist
create table if not exists public.radiology_worklist (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists radiology_worklist_data_gin on public.radiology_worklist using gin (data);
create index if not exists radiology_worklist_status_idx on public.radiology_worklist (status);

drop trigger if exists radiology_worklist_set_updated_at on public.radiology_worklist;
create trigger radiology_worklist_set_updated_at before update on public.radiology_worklist
  for each row execute function public.set_updated_at();

alter table public.radiology_worklist enable row level security;
drop policy if exists anon_all on public.radiology_worklist;
drop policy if exists authenticated_all on public.radiology_worklist;
create policy authenticated_all on public.radiology_worklist
  for all
  to authenticated
  using (true)
  with check (true);
-- specimen-tracking-service → /rest/v1/specimen_tracking
create table if not exists public.specimen_tracking (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists specimen_tracking_data_gin on public.specimen_tracking using gin (data);
create index if not exists specimen_tracking_status_idx on public.specimen_tracking (status);

drop trigger if exists specimen_tracking_set_updated_at on public.specimen_tracking;
create trigger specimen_tracking_set_updated_at before update on public.specimen_tracking
  for each row execute function public.set_updated_at();

alter table public.specimen_tracking enable row level security;
drop policy if exists anon_all on public.specimen_tracking;
drop policy if exists authenticated_all on public.specimen_tracking;
create policy authenticated_all on public.specimen_tracking
  for all
  to authenticated
  using (true)
  with check (true);
-- reference-ranges-service → /rest/v1/reference_ranges
create table if not exists public.reference_ranges (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists reference_ranges_data_gin on public.reference_ranges using gin (data);
create index if not exists reference_ranges_status_idx on public.reference_ranges (status);

drop trigger if exists reference_ranges_set_updated_at on public.reference_ranges;
create trigger reference_ranges_set_updated_at before update on public.reference_ranges
  for each row execute function public.set_updated_at();

alter table public.reference_ranges enable row level security;
drop policy if exists anon_all on public.reference_ranges;
drop policy if exists authenticated_all on public.reference_ranges;
create policy authenticated_all on public.reference_ranges
  for all
  to authenticated
  using (true)
  with check (true);
-- pharmacy-service → /rest/v1/pharmacy
create table if not exists public.pharmacy (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists pharmacy_data_gin on public.pharmacy using gin (data);
create index if not exists pharmacy_status_idx on public.pharmacy (status);

drop trigger if exists pharmacy_set_updated_at on public.pharmacy;
create trigger pharmacy_set_updated_at before update on public.pharmacy
  for each row execute function public.set_updated_at();

alter table public.pharmacy enable row level security;
drop policy if exists anon_all on public.pharmacy;
drop policy if exists authenticated_all on public.pharmacy;
create policy authenticated_all on public.pharmacy
  for all
  to authenticated
  using (true)
  with check (true);
-- prescriptions-service → /rest/v1/prescriptions
create table if not exists public.prescriptions (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists prescriptions_data_gin on public.prescriptions using gin (data);
create index if not exists prescriptions_status_idx on public.prescriptions (status);

drop trigger if exists prescriptions_set_updated_at on public.prescriptions;
create trigger prescriptions_set_updated_at before update on public.prescriptions
  for each row execute function public.set_updated_at();

alter table public.prescriptions enable row level security;
drop policy if exists anon_all on public.prescriptions;
drop policy if exists authenticated_all on public.prescriptions;
create policy authenticated_all on public.prescriptions
  for all
  to authenticated
  using (true)
  with check (true);
-- refills-service → /rest/v1/refills
create table if not exists public.refills (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists refills_data_gin on public.refills using gin (data);
create index if not exists refills_status_idx on public.refills (status);

drop trigger if exists refills_set_updated_at on public.refills;
create trigger refills_set_updated_at before update on public.refills
  for each row execute function public.set_updated_at();

alter table public.refills enable row level security;
drop policy if exists anon_all on public.refills;
drop policy if exists authenticated_all on public.refills;
create policy authenticated_all on public.refills
  for all
  to authenticated
  using (true)
  with check (true);
-- drug-interactions-service → /rest/v1/drug_interactions
create table if not exists public.drug_interactions (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists drug_interactions_data_gin on public.drug_interactions using gin (data);
create index if not exists drug_interactions_status_idx on public.drug_interactions (status);

drop trigger if exists drug_interactions_set_updated_at on public.drug_interactions;
create trigger drug_interactions_set_updated_at before update on public.drug_interactions
  for each row execute function public.set_updated_at();

alter table public.drug_interactions enable row level security;
drop policy if exists anon_all on public.drug_interactions;
drop policy if exists authenticated_all on public.drug_interactions;
create policy authenticated_all on public.drug_interactions
  for all
  to authenticated
  using (true)
  with check (true);
-- formulary-service → /rest/v1/formulary
create table if not exists public.formulary (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists formulary_data_gin on public.formulary using gin (data);
create index if not exists formulary_status_idx on public.formulary (status);

drop trigger if exists formulary_set_updated_at on public.formulary;
create trigger formulary_set_updated_at before update on public.formulary
  for each row execute function public.set_updated_at();

alter table public.formulary enable row level security;
drop policy if exists anon_all on public.formulary;
drop policy if exists authenticated_all on public.formulary;
create policy authenticated_all on public.formulary
  for all
  to authenticated
  using (true)
  with check (true);
-- pharmacy-inventory-service → /rest/v1/pharmacy_inventory
create table if not exists public.pharmacy_inventory (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists pharmacy_inventory_data_gin on public.pharmacy_inventory using gin (data);
create index if not exists pharmacy_inventory_status_idx on public.pharmacy_inventory (status);

drop trigger if exists pharmacy_inventory_set_updated_at on public.pharmacy_inventory;
create trigger pharmacy_inventory_set_updated_at before update on public.pharmacy_inventory
  for each row execute function public.set_updated_at();

alter table public.pharmacy_inventory enable row level security;
drop policy if exists anon_all on public.pharmacy_inventory;
drop policy if exists authenticated_all on public.pharmacy_inventory;
create policy authenticated_all on public.pharmacy_inventory
  for all
  to authenticated
  using (true)
  with check (true);
-- dispensing-service → /rest/v1/dispensing
create table if not exists public.dispensing (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists dispensing_data_gin on public.dispensing using gin (data);
create index if not exists dispensing_status_idx on public.dispensing (status);

drop trigger if exists dispensing_set_updated_at on public.dispensing;
create trigger dispensing_set_updated_at before update on public.dispensing
  for each row execute function public.set_updated_at();

alter table public.dispensing enable row level security;
drop policy if exists anon_all on public.dispensing;
drop policy if exists authenticated_all on public.dispensing;
create policy authenticated_all on public.dispensing
  for all
  to authenticated
  using (true)
  with check (true);
-- appointments-service → /rest/v1/appointments
create table if not exists public.appointments (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists appointments_data_gin on public.appointments using gin (data);
create index if not exists appointments_status_idx on public.appointments (status);

drop trigger if exists appointments_set_updated_at on public.appointments;
create trigger appointments_set_updated_at before update on public.appointments
  for each row execute function public.set_updated_at();

alter table public.appointments enable row level security;
drop policy if exists anon_all on public.appointments;
drop policy if exists authenticated_all on public.appointments;
create policy authenticated_all on public.appointments
  for all
  to authenticated
  using (true)
  with check (true);
-- appointment-slots-service → /rest/v1/appointment_slots
create table if not exists public.appointment_slots (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists appointment_slots_data_gin on public.appointment_slots using gin (data);
create index if not exists appointment_slots_status_idx on public.appointment_slots (status);

drop trigger if exists appointment_slots_set_updated_at on public.appointment_slots;
create trigger appointment_slots_set_updated_at before update on public.appointment_slots
  for each row execute function public.set_updated_at();

alter table public.appointment_slots enable row level security;
drop policy if exists anon_all on public.appointment_slots;
drop policy if exists authenticated_all on public.appointment_slots;
create policy authenticated_all on public.appointment_slots
  for all
  to authenticated
  using (true)
  with check (true);
-- reminders-service → /rest/v1/reminders
create table if not exists public.reminders (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists reminders_data_gin on public.reminders using gin (data);
create index if not exists reminders_status_idx on public.reminders (status);

drop trigger if exists reminders_set_updated_at on public.reminders;
create trigger reminders_set_updated_at before update on public.reminders
  for each row execute function public.set_updated_at();

alter table public.reminders enable row level security;
drop policy if exists anon_all on public.reminders;
drop policy if exists authenticated_all on public.reminders;
create policy authenticated_all on public.reminders
  for all
  to authenticated
  using (true)
  with check (true);
-- waitlist-service → /rest/v1/waitlist
create table if not exists public.waitlist (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists waitlist_data_gin on public.waitlist using gin (data);
create index if not exists waitlist_status_idx on public.waitlist (status);

drop trigger if exists waitlist_set_updated_at on public.waitlist;
create trigger waitlist_set_updated_at before update on public.waitlist
  for each row execute function public.set_updated_at();

alter table public.waitlist enable row level security;
drop policy if exists anon_all on public.waitlist;
drop policy if exists authenticated_all on public.waitlist;
create policy authenticated_all on public.waitlist
  for all
  to authenticated
  using (true)
  with check (true);
-- room-booking-service → /rest/v1/room_booking
create table if not exists public.room_booking (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists room_booking_data_gin on public.room_booking using gin (data);
create index if not exists room_booking_status_idx on public.room_booking (status);

drop trigger if exists room_booking_set_updated_at on public.room_booking;
create trigger room_booking_set_updated_at before update on public.room_booking
  for each row execute function public.set_updated_at();

alter table public.room_booking enable row level security;
drop policy if exists anon_all on public.room_booking;
drop policy if exists authenticated_all on public.room_booking;
create policy authenticated_all on public.room_booking
  for all
  to authenticated
  using (true)
  with check (true);
-- billing-service → /rest/v1/billing
create table if not exists public.billing (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists billing_data_gin on public.billing using gin (data);
create index if not exists billing_status_idx on public.billing (status);

drop trigger if exists billing_set_updated_at on public.billing;
create trigger billing_set_updated_at before update on public.billing
  for each row execute function public.set_updated_at();

alter table public.billing enable row level security;
drop policy if exists anon_all on public.billing;
drop policy if exists authenticated_all on public.billing;
create policy authenticated_all on public.billing
  for all
  to authenticated
  using (true)
  with check (true);
-- charge-capture-service → /rest/v1/charge_capture
create table if not exists public.charge_capture (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists charge_capture_data_gin on public.charge_capture using gin (data);
create index if not exists charge_capture_status_idx on public.charge_capture (status);

drop trigger if exists charge_capture_set_updated_at on public.charge_capture;
create trigger charge_capture_set_updated_at before update on public.charge_capture
  for each row execute function public.set_updated_at();

alter table public.charge_capture enable row level security;
drop policy if exists anon_all on public.charge_capture;
drop policy if exists authenticated_all on public.charge_capture;
create policy authenticated_all on public.charge_capture
  for all
  to authenticated
  using (true)
  with check (true);
-- coding-service → /rest/v1/coding
create table if not exists public.coding (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists coding_data_gin on public.coding using gin (data);
create index if not exists coding_status_idx on public.coding (status);

drop trigger if exists coding_set_updated_at on public.coding;
create trigger coding_set_updated_at before update on public.coding
  for each row execute function public.set_updated_at();

alter table public.coding enable row level security;
drop policy if exists anon_all on public.coding;
drop policy if exists authenticated_all on public.coding;
create policy authenticated_all on public.coding
  for all
  to authenticated
  using (true)
  with check (true);
-- claims-submission-service → /rest/v1/claims_submission
create table if not exists public.claims_submission (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists claims_submission_data_gin on public.claims_submission using gin (data);
create index if not exists claims_submission_status_idx on public.claims_submission (status);

drop trigger if exists claims_submission_set_updated_at on public.claims_submission;
create trigger claims_submission_set_updated_at before update on public.claims_submission
  for each row execute function public.set_updated_at();

alter table public.claims_submission enable row level security;
drop policy if exists anon_all on public.claims_submission;
drop policy if exists authenticated_all on public.claims_submission;
create policy authenticated_all on public.claims_submission
  for all
  to authenticated
  using (true)
  with check (true);
-- claims-adjudication-service → /rest/v1/claims_adjudication
create table if not exists public.claims_adjudication (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists claims_adjudication_data_gin on public.claims_adjudication using gin (data);
create index if not exists claims_adjudication_status_idx on public.claims_adjudication (status);

drop trigger if exists claims_adjudication_set_updated_at on public.claims_adjudication;
create trigger claims_adjudication_set_updated_at before update on public.claims_adjudication
  for each row execute function public.set_updated_at();

alter table public.claims_adjudication enable row level security;
drop policy if exists anon_all on public.claims_adjudication;
drop policy if exists authenticated_all on public.claims_adjudication;
create policy authenticated_all on public.claims_adjudication
  for all
  to authenticated
  using (true)
  with check (true);
-- denials-service → /rest/v1/denials
create table if not exists public.denials (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists denials_data_gin on public.denials using gin (data);
create index if not exists denials_status_idx on public.denials (status);

drop trigger if exists denials_set_updated_at on public.denials;
create trigger denials_set_updated_at before update on public.denials
  for each row execute function public.set_updated_at();

alter table public.denials enable row level security;
drop policy if exists anon_all on public.denials;
drop policy if exists authenticated_all on public.denials;
create policy authenticated_all on public.denials
  for all
  to authenticated
  using (true)
  with check (true);
-- invoicing-service → /rest/v1/invoicing
create table if not exists public.invoicing (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists invoicing_data_gin on public.invoicing using gin (data);
create index if not exists invoicing_status_idx on public.invoicing (status);

drop trigger if exists invoicing_set_updated_at on public.invoicing;
create trigger invoicing_set_updated_at before update on public.invoicing
  for each row execute function public.set_updated_at();

alter table public.invoicing enable row level security;
drop policy if exists anon_all on public.invoicing;
drop policy if exists authenticated_all on public.invoicing;
create policy authenticated_all on public.invoicing
  for all
  to authenticated
  using (true)
  with check (true);
-- payments-service → /rest/v1/payments
create table if not exists public.payments (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists payments_data_gin on public.payments using gin (data);
create index if not exists payments_status_idx on public.payments (status);

drop trigger if exists payments_set_updated_at on public.payments;
create trigger payments_set_updated_at before update on public.payments
  for each row execute function public.set_updated_at();

alter table public.payments enable row level security;
drop policy if exists anon_all on public.payments;
drop policy if exists authenticated_all on public.payments;
create policy authenticated_all on public.payments
  for all
  to authenticated
  using (true)
  with check (true);
-- statements-service → /rest/v1/statements
create table if not exists public.statements (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists statements_data_gin on public.statements using gin (data);
create index if not exists statements_status_idx on public.statements (status);

drop trigger if exists statements_set_updated_at on public.statements;
create trigger statements_set_updated_at before update on public.statements
  for each row execute function public.set_updated_at();

alter table public.statements enable row level security;
drop policy if exists anon_all on public.statements;
drop policy if exists authenticated_all on public.statements;
create policy authenticated_all on public.statements
  for all
  to authenticated
  using (true)
  with check (true);
-- collections-service → /rest/v1/collections
create table if not exists public.collections (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists collections_data_gin on public.collections using gin (data);
create index if not exists collections_status_idx on public.collections (status);

drop trigger if exists collections_set_updated_at on public.collections;
create trigger collections_set_updated_at before update on public.collections
  for each row execute function public.set_updated_at();

alter table public.collections enable row level security;
drop policy if exists anon_all on public.collections;
drop policy if exists authenticated_all on public.collections;
create policy authenticated_all on public.collections
  for all
  to authenticated
  using (true)
  with check (true);
-- eligibility-service → /rest/v1/eligibility
create table if not exists public.eligibility (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists eligibility_data_gin on public.eligibility using gin (data);
create index if not exists eligibility_status_idx on public.eligibility (status);

drop trigger if exists eligibility_set_updated_at on public.eligibility;
create trigger eligibility_set_updated_at before update on public.eligibility
  for each row execute function public.set_updated_at();

alter table public.eligibility enable row level security;
drop policy if exists anon_all on public.eligibility;
drop policy if exists authenticated_all on public.eligibility;
create policy authenticated_all on public.eligibility
  for all
  to authenticated
  using (true)
  with check (true);
-- prior-auth-service → /rest/v1/prior_auth
create table if not exists public.prior_auth (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists prior_auth_data_gin on public.prior_auth using gin (data);
create index if not exists prior_auth_status_idx on public.prior_auth (status);

drop trigger if exists prior_auth_set_updated_at on public.prior_auth;
create trigger prior_auth_set_updated_at before update on public.prior_auth
  for each row execute function public.set_updated_at();

alter table public.prior_auth enable row level security;
drop policy if exists anon_all on public.prior_auth;
drop policy if exists authenticated_all on public.prior_auth;
create policy authenticated_all on public.prior_auth
  for all
  to authenticated
  using (true)
  with check (true);
-- coverage-verification-service → /rest/v1/coverage_verification
create table if not exists public.coverage_verification (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists coverage_verification_data_gin on public.coverage_verification using gin (data);
create index if not exists coverage_verification_status_idx on public.coverage_verification (status);

drop trigger if exists coverage_verification_set_updated_at on public.coverage_verification;
create trigger coverage_verification_set_updated_at before update on public.coverage_verification
  for each row execute function public.set_updated_at();

alter table public.coverage_verification enable row level security;
drop policy if exists anon_all on public.coverage_verification;
drop policy if exists authenticated_all on public.coverage_verification;
create policy authenticated_all on public.coverage_verification
  for all
  to authenticated
  using (true)
  with check (true);
-- payer-directory → /rest/v1/payer_directory
create table if not exists public.payer_directory (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists payer_directory_data_gin on public.payer_directory using gin (data);
create index if not exists payer_directory_status_idx on public.payer_directory (status);

drop trigger if exists payer_directory_set_updated_at on public.payer_directory;
create trigger payer_directory_set_updated_at before update on public.payer_directory
  for each row execute function public.set_updated_at();

alter table public.payer_directory enable row level security;
drop policy if exists anon_all on public.payer_directory;
drop policy if exists authenticated_all on public.payer_directory;
create policy authenticated_all on public.payer_directory
  for all
  to authenticated
  using (true)
  with check (true);
-- payer-edi-connect → /rest/v1/payer_edi_connect
create table if not exists public.payer_edi_connect (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists payer_edi_connect_data_gin on public.payer_edi_connect using gin (data);
create index if not exists payer_edi_connect_status_idx on public.payer_edi_connect (status);

drop trigger if exists payer_edi_connect_set_updated_at on public.payer_edi_connect;
create trigger payer_edi_connect_set_updated_at before update on public.payer_edi_connect
  for each row execute function public.set_updated_at();

alter table public.payer_edi_connect enable row level security;
drop policy if exists anon_all on public.payer_edi_connect;
drop policy if exists authenticated_all on public.payer_edi_connect;
create policy authenticated_all on public.payer_edi_connect
  for all
  to authenticated
  using (true)
  with check (true);
-- claims-status-service → /rest/v1/claims_status
create table if not exists public.claims_status (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists claims_status_data_gin on public.claims_status using gin (data);
create index if not exists claims_status_status_idx on public.claims_status (status);

drop trigger if exists claims_status_set_updated_at on public.claims_status;
create trigger claims_status_set_updated_at before update on public.claims_status
  for each row execute function public.set_updated_at();

alter table public.claims_status enable row level security;
drop policy if exists anon_all on public.claims_status;
drop policy if exists authenticated_all on public.claims_status;
create policy authenticated_all on public.claims_status
  for all
  to authenticated
  using (true)
  with check (true);
-- device-registry-service → /rest/v1/device_registry
create table if not exists public.device_registry (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists device_registry_data_gin on public.device_registry using gin (data);
create index if not exists device_registry_status_idx on public.device_registry (status);

drop trigger if exists device_registry_set_updated_at on public.device_registry;
create trigger device_registry_set_updated_at before update on public.device_registry
  for each row execute function public.set_updated_at();

alter table public.device_registry enable row level security;
drop policy if exists anon_all on public.device_registry;
drop policy if exists authenticated_all on public.device_registry;
create policy authenticated_all on public.device_registry
  for all
  to authenticated
  using (true)
  with check (true);
-- device-alerts-service → /rest/v1/device_alerts
create table if not exists public.device_alerts (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists device_alerts_data_gin on public.device_alerts using gin (data);
create index if not exists device_alerts_status_idx on public.device_alerts (status);

drop trigger if exists device_alerts_set_updated_at on public.device_alerts;
create trigger device_alerts_set_updated_at before update on public.device_alerts
  for each row execute function public.set_updated_at();

alter table public.device_alerts enable row level security;
drop policy if exists anon_all on public.device_alerts;
drop policy if exists authenticated_all on public.device_alerts;
create policy authenticated_all on public.device_alerts
  for all
  to authenticated
  using (true)
  with check (true);
-- device-fleet-service → /rest/v1/device_fleet
create table if not exists public.device_fleet (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists device_fleet_data_gin on public.device_fleet using gin (data);
create index if not exists device_fleet_status_idx on public.device_fleet (status);

drop trigger if exists device_fleet_set_updated_at on public.device_fleet;
create trigger device_fleet_set_updated_at before update on public.device_fleet
  for each row execute function public.set_updated_at();

alter table public.device_fleet enable row level security;
drop policy if exists anon_all on public.device_fleet;
drop policy if exists authenticated_all on public.device_fleet;
create policy authenticated_all on public.device_fleet
  for all
  to authenticated
  using (true)
  with check (true);
-- remote-monitoring-service → /rest/v1/remote_monitoring
create table if not exists public.remote_monitoring (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists remote_monitoring_data_gin on public.remote_monitoring using gin (data);
create index if not exists remote_monitoring_status_idx on public.remote_monitoring (status);

drop trigger if exists remote_monitoring_set_updated_at on public.remote_monitoring;
create trigger remote_monitoring_set_updated_at before update on public.remote_monitoring
  for each row execute function public.set_updated_at();

alter table public.remote_monitoring enable row level security;
drop policy if exists anon_all on public.remote_monitoring;
drop policy if exists authenticated_all on public.remote_monitoring;
create policy authenticated_all on public.remote_monitoring
  for all
  to authenticated
  using (true)
  with check (true);
-- notifications-service → /rest/v1/notifications
create table if not exists public.notifications (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists notifications_data_gin on public.notifications using gin (data);
create index if not exists notifications_status_idx on public.notifications (status);

drop trigger if exists notifications_set_updated_at on public.notifications;
create trigger notifications_set_updated_at before update on public.notifications
  for each row execute function public.set_updated_at();

alter table public.notifications enable row level security;
drop policy if exists anon_all on public.notifications;
drop policy if exists authenticated_all on public.notifications;
create policy authenticated_all on public.notifications
  for all
  to authenticated
  using (true)
  with check (true);
-- email-gateway-service → /rest/v1/email_gateway
create table if not exists public.email_gateway (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists email_gateway_data_gin on public.email_gateway using gin (data);
create index if not exists email_gateway_status_idx on public.email_gateway (status);

drop trigger if exists email_gateway_set_updated_at on public.email_gateway;
create trigger email_gateway_set_updated_at before update on public.email_gateway
  for each row execute function public.set_updated_at();

alter table public.email_gateway enable row level security;
drop policy if exists anon_all on public.email_gateway;
drop policy if exists authenticated_all on public.email_gateway;
create policy authenticated_all on public.email_gateway
  for all
  to authenticated
  using (true)
  with check (true);
-- push-gateway-service → /rest/v1/push_gateway
create table if not exists public.push_gateway (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists push_gateway_data_gin on public.push_gateway using gin (data);
create index if not exists push_gateway_status_idx on public.push_gateway (status);

drop trigger if exists push_gateway_set_updated_at on public.push_gateway;
create trigger push_gateway_set_updated_at before update on public.push_gateway
  for each row execute function public.set_updated_at();

alter table public.push_gateway enable row level security;
drop policy if exists anon_all on public.push_gateway;
drop policy if exists authenticated_all on public.push_gateway;
create policy authenticated_all on public.push_gateway
  for all
  to authenticated
  using (true)
  with check (true);
-- ai-agents-service → /rest/v1/ai_agents
create table if not exists public.ai_agents (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists ai_agents_data_gin on public.ai_agents using gin (data);
create index if not exists ai_agents_status_idx on public.ai_agents (status);

drop trigger if exists ai_agents_set_updated_at on public.ai_agents;
create trigger ai_agents_set_updated_at before update on public.ai_agents
  for each row execute function public.set_updated_at();

alter table public.ai_agents enable row level security;
drop policy if exists anon_all on public.ai_agents;
drop policy if exists authenticated_all on public.ai_agents;
create policy authenticated_all on public.ai_agents
  for all
  to authenticated
  using (true)
  with check (true);
-- ai-invocations-service → /rest/v1/ai_invocations
create table if not exists public.ai_invocations (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists ai_invocations_data_gin on public.ai_invocations using gin (data);
create index if not exists ai_invocations_status_idx on public.ai_invocations (status);

drop trigger if exists ai_invocations_set_updated_at on public.ai_invocations;
create trigger ai_invocations_set_updated_at before update on public.ai_invocations
  for each row execute function public.set_updated_at();

alter table public.ai_invocations enable row level security;
drop policy if exists anon_all on public.ai_invocations;
drop policy if exists authenticated_all on public.ai_invocations;
create policy authenticated_all on public.ai_invocations
  for all
  to authenticated
  using (true)
  with check (true);
-- analytics-events-service → /rest/v1/analytics_events
create table if not exists public.analytics_events (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists analytics_events_data_gin on public.analytics_events using gin (data);
create index if not exists analytics_events_status_idx on public.analytics_events (status);

drop trigger if exists analytics_events_set_updated_at on public.analytics_events;
create trigger analytics_events_set_updated_at before update on public.analytics_events
  for each row execute function public.set_updated_at();

alter table public.analytics_events enable row level security;
drop policy if exists anon_all on public.analytics_events;
drop policy if exists authenticated_all on public.analytics_events;
create policy authenticated_all on public.analytics_events
  for all
  to authenticated
  using (true)
  with check (true);
-- reporting-service → /rest/v1/reporting
create table if not exists public.reporting (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists reporting_data_gin on public.reporting using gin (data);
create index if not exists reporting_status_idx on public.reporting (status);

drop trigger if exists reporting_set_updated_at on public.reporting;
create trigger reporting_set_updated_at before update on public.reporting
  for each row execute function public.set_updated_at();

alter table public.reporting enable row level security;
drop policy if exists anon_all on public.reporting;
drop policy if exists authenticated_all on public.reporting;
create policy authenticated_all on public.reporting
  for all
  to authenticated
  using (true)
  with check (true);
-- ml-models-service → /rest/v1/ml_models
create table if not exists public.ml_models (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists ml_models_data_gin on public.ml_models using gin (data);
create index if not exists ml_models_status_idx on public.ml_models (status);

drop trigger if exists ml_models_set_updated_at on public.ml_models;
create trigger ml_models_set_updated_at before update on public.ml_models
  for each row execute function public.set_updated_at();

alter table public.ml_models enable row level security;
drop policy if exists anon_all on public.ml_models;
drop policy if exists authenticated_all on public.ml_models;
create policy authenticated_all on public.ml_models
  for all
  to authenticated
  using (true)
  with check (true);
-- erp-bridge-service → /rest/v1/erp_bridge
create table if not exists public.erp_bridge (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists erp_bridge_data_gin on public.erp_bridge using gin (data);
create index if not exists erp_bridge_status_idx on public.erp_bridge (status);

drop trigger if exists erp_bridge_set_updated_at on public.erp_bridge;
create trigger erp_bridge_set_updated_at before update on public.erp_bridge
  for each row execute function public.set_updated_at();

alter table public.erp_bridge enable row level security;
drop policy if exists anon_all on public.erp_bridge;
drop policy if exists authenticated_all on public.erp_bridge;
create policy authenticated_all on public.erp_bridge
  for all
  to authenticated
  using (true)
  with check (true);
-- facilities-service → /rest/v1/facilities
create table if not exists public.facilities (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists facilities_data_gin on public.facilities using gin (data);
create index if not exists facilities_status_idx on public.facilities (status);

drop trigger if exists facilities_set_updated_at on public.facilities;
create trigger facilities_set_updated_at before update on public.facilities
  for each row execute function public.set_updated_at();

alter table public.facilities enable row level security;
drop policy if exists anon_all on public.facilities;
drop policy if exists authenticated_all on public.facilities;
create policy authenticated_all on public.facilities
  for all
  to authenticated
  using (true)
  with check (true);
-- wards-beds-service → /rest/v1/wards_beds
create table if not exists public.wards_beds (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists wards_beds_data_gin on public.wards_beds using gin (data);
create index if not exists wards_beds_status_idx on public.wards_beds (status);

drop trigger if exists wards_beds_set_updated_at on public.wards_beds;
create trigger wards_beds_set_updated_at before update on public.wards_beds
  for each row execute function public.set_updated_at();

alter table public.wards_beds enable row level security;
drop policy if exists anon_all on public.wards_beds;
drop policy if exists authenticated_all on public.wards_beds;
create policy authenticated_all on public.wards_beds
  for all
  to authenticated
  using (true)
  with check (true);
-- equipment-service → /rest/v1/equipment
create table if not exists public.equipment (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists equipment_data_gin on public.equipment using gin (data);
create index if not exists equipment_status_idx on public.equipment (status);

drop trigger if exists equipment_set_updated_at on public.equipment;
create trigger equipment_set_updated_at before update on public.equipment
  for each row execute function public.set_updated_at();

alter table public.equipment enable row level security;
drop policy if exists anon_all on public.equipment;
drop policy if exists authenticated_all on public.equipment;
create policy authenticated_all on public.equipment
  for all
  to authenticated
  using (true)
  with check (true);
-- sterile-supply-service → /rest/v1/sterile_supply
create table if not exists public.sterile_supply (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists sterile_supply_data_gin on public.sterile_supply using gin (data);
create index if not exists sterile_supply_status_idx on public.sterile_supply (status);

drop trigger if exists sterile_supply_set_updated_at on public.sterile_supply;
create trigger sterile_supply_set_updated_at before update on public.sterile_supply
  for each row execute function public.set_updated_at();

alter table public.sterile_supply enable row level security;
drop policy if exists anon_all on public.sterile_supply;
drop policy if exists authenticated_all on public.sterile_supply;
create policy authenticated_all on public.sterile_supply
  for all
  to authenticated
  using (true)
  with check (true);
-- maintenance-service → /rest/v1/maintenance
create table if not exists public.maintenance (
  id           bigserial primary key,
  data         jsonb not null default '{}'::jsonb,
  status       text not null default 'active',
  created_at   timestamptz not null default now(),
  updated_at   timestamptz not null default now()
);
create index if not exists maintenance_data_gin on public.maintenance using gin (data);
create index if not exists maintenance_status_idx on public.maintenance (status);

drop trigger if exists maintenance_set_updated_at on public.maintenance;
create trigger maintenance_set_updated_at before update on public.maintenance
  for each row execute function public.set_updated_at();

alter table public.maintenance enable row level security;
drop policy if exists anon_all on public.maintenance;
drop policy if exists authenticated_all on public.maintenance;
create policy authenticated_all on public.maintenance
  for all
  to authenticated
  using (true)
  with check (true);

-- ── Seed data so the frontend has something to show ───────────────────────
insert into public.patients (first_name, last_name, dob, mrn, data) values
  ('Ava',    'Reyes',    '1988-03-14', 'MRN-001', '{"email":"ava.reyes@example.com","phone":"+1-555-0110","blood_type":"O+"}'),
  ('Julian', 'Okafor',   '1975-11-02', 'MRN-002', '{"email":"julian.okafor@example.com","phone":"+1-555-0211","blood_type":"A-"}'),
  ('Priya',  'Shah',     '1992-06-21', 'MRN-003', '{"email":"priya.shah@example.com","phone":"+1-555-0322","blood_type":"B+"}'),
  ('Ada',    'Lovelace', '1815-12-10', 'MRN-004', '{"email":"ada@analytical.engine","phone":"+1-555-0433","blood_type":"AB+"}')
on conflict (mrn) do nothing;

insert into public.providers (data) values
  ('{"first_name":"Dr. Sarah","last_name":"Chen","npi":"1234567890","specialty":"Family Medicine","employee_id":"EMP-101"}'::jsonb),
  ('{"first_name":"Dr. Marcus","last_name":"Rodriguez","npi":"1234567891","specialty":"Cardiology","employee_id":"EMP-102"}'::jsonb),
  ('{"first_name":"Dr. Aisha","last_name":"Patel","npi":"1234567892","specialty":"Pediatrics","employee_id":"EMP-103"}'::jsonb)
on conflict do nothing;

insert into public.encounters (data) values
  ('{"patient_id":1,"provider_id":1,"reason":"Annual physical","location":"Clinic A","status":"completed"}'::jsonb),
  ('{"patient_id":1,"provider_id":2,"reason":"Cardiology follow-up","location":"Clinic B"}'::jsonb),
  ('{"patient_id":2,"provider_id":1,"reason":"Blood pressure check"}'::jsonb),
  ('{"patient_id":3,"provider_id":3,"reason":"Pediatric wellness"}'::jsonb)
on conflict do nothing;

insert into public.appointments (data) values
  ('{"patient_id":1,"provider_id":1,"starts_at":"2026-08-15T10:00:00Z","duration_min":30,"reason":"Annual physical"}'::jsonb),
  ('{"patient_id":2,"provider_id":2,"starts_at":"2026-08-15T14:00:00Z","duration_min":45,"reason":"Follow-up"}'::jsonb),
  ('{"patient_id":1,"provider_id":2,"starts_at":"2026-08-20T09:00:00Z","duration_min":30,"reason":"Cardiology"}'::jsonb),
  ('{"patient_id":3,"provider_id":3,"starts_at":"2026-08-16T11:00:00Z","duration_min":30,"reason":"Wellness"}'::jsonb)
on conflict do nothing;

insert into public.prescriptions (data) values
  ('{"patient_id":1,"provider_id":1,"drug":"Lisinopril","dose":"10mg","sig":"1 tab daily"}'::jsonb),
  ('{"patient_id":1,"provider_id":2,"drug":"Metoprolol","dose":"25mg","sig":"1 tab twice daily"}'::jsonb),
  ('{"patient_id":2,"provider_id":1,"drug":"Amlodipine","dose":"5mg","sig":"1 tab daily"}'::jsonb)
on conflict do nothing;

insert into public.lab_orders (data) values
  ('{"patient_id":1,"ordered_by":1,"test_code":"CBC","priority":"routine"}'::jsonb),
  ('{"patient_id":1,"ordered_by":2,"test_code":"LIPID","priority":"routine"}'::jsonb),
  ('{"patient_id":2,"ordered_by":1,"test_code":"CMP","priority":"stat"}'::jsonb)
on conflict do nothing;

insert into public.lab_results (data) values
  ('{"patient_id":1,"lab_order_id":1,"test_code":"CBC","result":"Normal","hemoglobin":14.2}'::jsonb),
  ('{"patient_id":1,"lab_order_id":2,"test_code":"LIPID","result":"Elevated LDL","ldl":145}'::jsonb)
on conflict do nothing;

insert into public.invoicing (data) values
  ('{"patient_id":1,"amount":250.00,"description":"Annual physical - 2026","status":"pending"}'::jsonb),
  ('{"patient_id":1,"amount":85.50,"description":"Cardiology consult","status":"pending"}'::jsonb),
  ('{"patient_id":2,"amount":150.00,"description":"BP check + labs","status":"paid"}'::jsonb)
on conflict do nothing;
